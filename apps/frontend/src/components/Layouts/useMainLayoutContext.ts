// Placeholder hook for future shared layout state (selected channel etc.)
// Exported separately to keep MainLayout file component-only for fast refresh.
export const useMainLayoutContext = () => {
  return {} as const;
};
